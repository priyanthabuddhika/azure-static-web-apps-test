
name: workflow test

on:
  workflow_dispatch

  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Show GitHub context   
      env: 
        GITHUB_CONTEXT: ${{ toJson(github) }}   
      if: contains('["priyanthabuddhika","herath"]', github.actor)
      run: echo "$GITHUB_CONTEXT"
      shell: bash
      
    - name: Show haspermission   
      run: echo ${{ env.permission }}
      shell: bash

